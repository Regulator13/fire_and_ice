/// @description create life
if (tScore >= lastLifeScore+global.scoreLife) {
    //tLives += 1; //if a goal has already been set, add a life
    lastLifeScore += global.scoreLife; //set next score goal
    }

